# 依赖
node_modules/

# 本地配置（含真实 API key，禁止提交）
config.local.js

# 运行时日志与号池数据库（logs/ 整体不提交，含密钥与统计）
logs/
*.log
upstreams.db
upstreams.db-shm
upstreams.db-wal

# 系统文件
.DS_Store

# 第三方工具自动注入的 skill 链接（与本仓库无关，指向本机路径）
.aionrs/
